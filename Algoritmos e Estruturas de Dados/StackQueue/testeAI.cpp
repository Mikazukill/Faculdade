/*
int main () {
    QueueDoubleLinked fila;

    cout << "--- TESTANDO A FILA ---" << endl;

    // 1. Testando se começa vazia (ANTES DA ADIÇÃO)
    cout << "1. ANTES de adicionar: a fila esta vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;

    // 2. Inserindo o primeiro elemento
    cout << "\n2. Inserindo o elemento 10..." << endl;
    fila.enqueue(10);int main () {
    QueueDoubleLinked fila;

    cout << "--- TESTANDO A FILA ---" << endl;

    // 1. Testando se começa vazia (ANTES DA ADIÇÃO)
    cout << "1. ANTES de adicionar: a fila esta vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;

    // 2. Inserindo o primeiro elemento
    cout << "\n2. Inserindo o elemento 10..." << endl;
    fila.enqueue(10);
    
    // 3. Testando de novo (DEPOIS DA ADIÇÃO)
    cout << "3. DEPOIS de adicionar: a fila esta vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;

    // 4. Inserindo mais elementos para continuar o teste
    cout << "\n4. Inserindo mais elementos: 20 e 30..." << endl;
    fila.enqueue(20);
    fila.enqueue(30);
    
    cout << "Conteudo da fila: " << fila.toString() << endl;
    cout << "Tamanho da fila: " << fila.size() << endl;

    // 5. Testando o primeiro elemento (Head)
    cout << "\n5. O primeiro da fila (head) deve ser 10. Resultado: " << fila.head() << endl;
int main () {
    QueueDoubleLinked fila;

    cout << "--- TESTANDO A FILA ---" << endl;

    // 1. Testando se começa vazia (ANTES DA ADIÇÃO)
    cout << "1. ANTES de adicionar: a fila esta vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;

    // 2. Inserindo o primeiro elemento
    cout << "\n2. Inserindo o elemento 10..." << endl;
    fila.enqueue(10);
    
    // 3. Testando de novo (DEPOIS DA ADIÇÃO)
    cout << "3. DEPOIS de adicionar: a fila esta vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;

    // 4. Inserindo mais elementos para continuar o teste
    cout << "\n4. Inserindo mais elementos: 20 e 30..." << endl;
    fila.enqueue(20);
    fila.enqueue(30);
    
    cout << "Conteudo da fila: " << fila.toString() << endl;
    cout << "Tamanho da fila: " << fila.size() << endl;

    // 5. Testando o primeiro elemento (Head)
    cout << "\n5. O primeiro da fila (head) deve ser 10. Resultado: " << fila.head() << endl;

    // 6. Testando remoção (Dequeue)
    cout << "\n6. Removendo o primeiro (dequeue)... Valor removido: " << fila.dequeue() << endl;
    cout << "Conteudo da fila apos remocao: " << fila.toString() << endl;
    cout << "Tamanho da fila agora: " << fila.size() << endl;

    // 7. Testando limpeza (Clear)
    cout << "\n7. Limpando a fila inteira..." << endl;
    fila.clear();
    cout << "Fila vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;
    cout << "Tamanho apos limpar: " << fila.size() << endl;

    return 0;
}
    // 6. Testando remoção (Dequeue)
    cout << "\n6. Removendo o primeiro (dequeue)... Valor removido: " << fila.dequeue() << endl;
    cout << "Conteudo da fila apos remocao: " << fila.toString() << endl;
    cout << "Tamanho da fila agora: " << fila.size() << endl;

    // 7. Testando limpeza (Clear)
    cout << "\n7. Limpando a fila inteira..." << endl;
    fila.clear();
    cout << "Fila vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;
    cout << "Tamanho apos limpar: " << fila.size() << endl;

    return 0;
}
    
    // 3. Testando de novo (DEPOIS DA ADIÇÃO)
    cout << "3. DEPOIS de adicionar: a fila esta vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;

    // 4. Inserindo mais elementos para continuar o teste
    cout << "\n4. Inserindo mais elementos: 20 e 30..." << endl;
    fila.enqueue(20);
    fila.enqueue(30);
    
    cout << "Conteudo da fila: " << fila.toString() << endl;
    cout << "Tamanho da fila: " << fila.size() << endl;

    // 5. Testando o primeiro elemento (Head)
    cout << "\n5. O primeiro da fila (head) deve ser 10. Resultado: " << fila.head() << endl;

    // 6. Testando remoção (Dequeue)
    cout << "\n6. Removendo o primeiro (dequeue)... Valor removido: " << fila.dequeue() << endl;
    cout << "Conteudo da fila apos remocao: " << fila.toString() << endl;
    cout << "Tamanho da fila agora: " << fila.size() << endl;

    // 7. Testando limpeza (Clear)
    cout << "\n7. Limpando a fila inteira..." << endl;
    fila.clear();
    cout << "Fila vazia? " << (fila.isEmpty() ? "Sim" : "Nao") << endl;
    cout << "Tamanho apos limpar: " << fila.size() << endl;

    return 0;
}
*/